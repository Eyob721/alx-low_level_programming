#ifndef MAIN_H
#define MAIN_H

#include <stdlib.h>

unsigned int binary_to_uint(const char *b);
void print_binary(unsigned long int n);

/* Library functions */
int _strlen(char *s);
int _putchar(char c);

#endif
